import React from 'react';
import ReactDOM from 'react-dom';
import App from './containers/App';

ReactDOM.render(<App />, document.getElementById('app'));

/* el initialState.json es una lista para hacer un API fake instalando el paquete de json Server: sudo npm install json-server -g el cual se va a encargar de hacer que este elemento sea como una API*/
